#--------------------------------------------------------------------#
# JOB CONTROL
# START_PRINT, END_PRINT, CANCEL_PRINT, PAUSE, RESUME
#--------------------------------------------------------------------#

[delayed_gcode _print_start_wait]
gcode:
    {% if printer['gcode_macro START_PRINT'].state == 'Heating'%}
        {action_respond_info("Prepare->Heating!")}
    {% elif printer['gcode_macro START_PRINT'].state == 'Start' %}
        {action_respond_info("Heating->Start!")}
    {% endif %}

    {% if printer['gcode_macro START_PRINT'].execute|lower != 'false' %}
        START_PRINT
    {% endif %}


[gcode_macro START_PRINT]
# description: Start the print with the correct temperature
variable_state: 'Prepare'
variable_record_extruder_temp: 0
variable_max_record_extruder_temp: 0
gcode:
    # Read temps from slicer params, fall back to defaults if not provided
    {% set extruder_target_temp = params.HOTEND|default(215)|int %}
    {% set params.EXTRUDER = extruder_target_temp %}
    PRINT_START

    
#     {% set bed_target_temp = params.BED|default(65)|int %}
 
#     {action_respond_info("Starting print - Extruder target: %d, current: %.1f - Bed target: %d, current: %.1f" % (extruder_target_temp, printer.extruder.temperature, bed_target_temp, printer.heater_bed.temperature))}
 
#     M400
#     CLEAR_PAUSE
#     G90
 
#     # Always home fresh - never trust a stale home from a previous print
#     G28
 
#     {% if printer['filament_switch_sensor filament_sensor'].enable == True and
#           printer['filament_switch_sensor filament_sensor'].filament_detected != True
#     %}
#         M117 No filament!!!
#         {action_respond_info("Please Insert filament in Sensor!")}
#         CANCEL_PRINT
#     {% endif %}
 
#     {action_respond_info("Check Heating!")}
 
#     M140 S{bed_target_temp}
#     M104 S{extruder_target_temp}
 
#     {% if printer.heater_bed.temperature < bed_target_temp %}
#         M117 Bed heating...
#         {action_respond_info("Bed heating - Target: %d, Current: %.1f" % (bed_target_temp, printer.heater_bed.temperature))}
#         M190 S{bed_target_temp}
#     {% endif %}
 
#     {action_respond_info("Before M109 - Extruder target: %d, current: %.1f - Bed target: %d, current: %.1f" % (extruder_target_temp, printer.extruder.temperature, bed_target_temp, printer.heater_bed.temperature))}
 
#     {% if printer.extruder.temperature < extruder_target_temp %}
#         M117 Nozzle heating...
#         {action_respond_info("Nozzle heating...")}
#         M109 S{extruder_target_temp}
#     {% endif %}
 
#     {action_respond_info("After M109 - Extruder target: %d, current: %.1f - Bed target: %d, current: %.1f" % (extruder_target_temp, printer.extruder.temperature, bed_target_temp, printer.heater_bed.temperature))}
 
#     # Wipe nozzle before QGL to ensure clean nozzle for probing
#     M117 Heating nozzle for wipe...
#     M109 S210
#     WIPER_RUN
#     M104 S{extruder_target_temp}  # set back to print temp, don't wait
 
#     # Always run QGL fresh - never trust a stale level from a previous print
#     QUAD_GANTRY_LEVEL
 
#     # Clear any old/corrupted mesh before taking a fresh one
#     BED_MESH_CLEAR
#     BED_MESH_CALIBRATE ADAPTIVE=1
 
#     # Final wipe at print temp before starting
#     M109 S{extruder_target_temp}
#     WIPER_RUN
 
#     {action_respond_info("Before print start - Extruder target: %d, current: %.1f - Bed target: %d, current: %.1f" % (extruder_target_temp, printer.extruder.temperature, bed_target_temp, printer.heater_bed.temperature))}

#     PURGE_LINE
    
#     M117 Printing now!!!
#     {action_respond_info("Start!")}

#####################################################################
#   A better print_start macro for v2/trident
#####################################################################

## *** THINGS TO UNCOMMENT: ***
## Bed mesh (2 lines at 2 locations)
## Nevermore (if you have one)
## Z_TILT_ADJUST (For Trident only)
## QUAD_GANTRY_LEVEL (For V2.4 only)

[gcode_macro PRINT_START]
variable_state: 'Prepare'
variable_record_extruder_temp: 0
variable_max_record_extruder_temp: 0
gcode:
  # This part fetches data from your slicer. Such as bed, extruder, and chamber temps and size of your printer.
  {% set target_bed = params.BED|int %}
  {% set target_extruder = params.EXTRUDER|int %}
  {% set target_chamber = params.CHAMBER|default("45")|int %}
  {% set x_wait = printer.toolhead.axis_maximum.x|float / 2 %}
  {% set y_wait = printer.toolhead.axis_maximum.y|float / 2 %}

  SET_GCODE_OFFSET Z=0                                 # Set offset to 0

  # Home the printer, set absolute positioning and update the Stealthburner LEDs.
  #STATUS_HOMING                                         # Set LEDs to homing-mode
  G28                                                   # Full home (XYZ)
  G90                                                   # Absolute position

  ##  Uncomment for bed mesh (1 of 2 for bed mesh)
  #BED_MESH_CLEAR                                       # Clear old saved bed mesh (if any)

  # Check if the bed temp is higher than 90c - if so then trigger a heatsoak.
  {% if params.BED|int > 90 %}
    SET_DISPLAY_TEXT MSG="Bed: {target_bed}c"           # Display info on display
    #STATUS_HEATING                                      # Set LEDs to heating-mode
    M106 S255                                           # Turn on the PT-fan

    ##  Uncomment if you have a Nevermore.
    #SET_PIN PIN=nevermore VALUE=1                      # Turn on the nevermore

    G1 X{x_wait} Y{y_wait} Z15 F9000                    # Go to center of the bed
    M190 S{target_bed}                                  # Set the target temp for the bed
    SET_DISPLAY_TEXT MSG="Heatsoak: {target_chamber}c"  # Display info on display
    TEMPERATURE_WAIT SENSOR="temperature_sensor chamber" MINIMUM={target_chamber}   # Waits for chamber temp

  # If the bed temp is not over 90c, then skip the heatsoak and just heat up to set temp with a 5 min soak
  {% else %}
    SET_DISPLAY_TEXT MSG="Bed: {target_bed}c"           # Display info on display
    #STATUS_HEATING                                      # Set LEDs to heating-mode
    G1 X{x_wait} Y{y_wait} Z15 F9000                    # Go to center of the bed
    M190 S{target_bed}                                  # Set the target temp for the bed
    SET_DISPLAY_TEXT MSG="Soak for 1 min"               # Display info on display
    G4 P60000                                          # Wait 1 min for the bedtemp to stabilize
  {% endif %}

  # Heat hotend to 150c. This helps with getting a correct Z-home.
  SET_DISPLAY_TEXT MSG="Hotend: 150c"                   # Display info on display
  M109 S150                                             # Heat hotend to 150c

  ##  Uncomment for Trident (Z_TILT_ADJUST)
  #SET_DISPLAY_TEXT MSG="Leveling"                      # Display info on display
  #STATUS_LEVELING                                      # Set LEDs to leveling-mode
  #Z_TILT_ADJUST                                        # Level the printer via Z_TILT_ADJUST
  #G28 Z                                                # Home Z again after Z_TILT_ADJUST

  ##  Uncomment for V2.4 (Quad gantry level AKA QGL)
  SET_DISPLAY_TEXT MSG="Leveling"                      # Display info on display
  #STATUS_LEVELING                                      # Set LEDs to leveling-mode
  QUAD_GANTRY_LEVEL                                    # Level the printer via QGL
  G28 Z                                                # Home Z again after QGL

  ##  Uncomment for bed mesh (2 of 2 for bed mesh)
  SET_DISPLAY_TEXT MSG="Bed mesh"                      # Display info on display
  #STATUS_MESHING                                       # Set LEDs to bed mesh-mode
  BED_MESH_CALIBRATE                                   # Start the bed mesh (add ADAPTIVE=1) for adaptive bed mesh
  WIPER_RUN
  M109 S150

  CARTOGRAPHER_TOUCH_HOME                              # Calibrate z offset only with hot nozzle

  # Heat up the hotend up to target via data from slicer
  SET_DISPLAY_TEXT MSG="Hotend: {target_extruder}c"     # Display info on display
  #STATUS_HEATING                                        # Set LEDs to heating-mode
  G1 X{x_wait} Y{y_wait} Z15 F9000                      # Go to center of the bed
  M107                                                  # Turn off partcooling fan
  M109 S{target_extruder}                               # Heat the hotend to set temp

  # Get ready to print by doing a primeline and updating the LEDs
  SET_DISPLAY_TEXT MSG="Printer goes brr"               # Display info on display
  #STATUS_PRINTING                                       # Set LEDs to printing-mode
  G0 X{x_wait - 50} Y4 F10000                           # Go to starting point
  G0 Z0.4                                               # Raise Z to 0.4
  G91                                                   # Incremental positioning 
  G1 X100 E20 F1000                                     # Primeline
  G90                                                   # Absolute position


[gcode_macro END_PRINT]
description: 
variable_state: 'normal'
gcode:
    {% set e_mintemp  = printer.configfile.settings['extruder'].min_extrude_temp %}

    M400
    _RESET_PRINT_STATE

    M117 Finish Print!!!
    G91
    {% if printer['filament_switch_sensor filament_sensor'].enable == True and
          printer['filament_switch_sensor filament_sensor'].filament_detected == True
    %}
        {% if (printer.extruder.target != 0 and printer.extruder.temperature >= printer.extruder.target) or
              printer.extruder.temperature >= e_mintemp
        %}
            G1 E-2 F2700
            G1 E-2 Z0.2 F2400
        {% endif %}
    {% endif %}

    _SAFE_Z_LIFT LIFT=10
    G1 X5 Y340 F9000

    _ALL_FAN_OFF
    TURN_OFF_HEATERS

    M84 X Y Z E  

    M220 S100
    M221 S100

    CLEAR_PAUSE

    {action_respond_info("Finish Print!")}


[gcode_macro _CLIENT_VARIABLE]
# Mainsail owns PAUSE, RESUME, and CANCEL_PRINT. These values preserve
# Hoss's previous parking locations and connect the runout sensor.
variable_use_custom_pos: True
variable_custom_park_x: 0
variable_custom_park_y: 0
variable_custom_park_dz: 5
variable_retract: 1
variable_cancel_retract: 1
variable_speed_retract: 5
variable_unretract: 1
variable_speed_unretract: 5
variable_speed_hop: 50
variable_speed_move: 150
variable_park_at_cancel: True
variable_park_at_cancel_x: 0
variable_park_at_cancel_y: 350
variable_runout_sensor: "filament_switch_sensor filament_sensor"

[gcode_macro PURGE_LINE]
description: Prime nozzle with purge line at front-left before print
gcode:
    M117 Purging...
    G90
    M83
    G0 X5 Y5 Z5 F9000        # move to front-left safely
    G0 Z0.25 F600             # drop to first layer height
    G1 X60 E10 F1000          # purge line
    G1 X65 F3000              # wipe tail
    G0 Z2 F3000               # lift
    M117 Purge complete
